<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Task</title>
</head>
<body>
  <h1>Edit Task</h1>
  <a href="/" class="home-button">Home</a>
  <form action="/admin/save-task" method="POST">
    <input type="hidden" name="id" value="<%= task.id %>">
    <label>Name:</label>
    <input type="text" name="name" value="<%= task.name %>" required>
    <br>
    <label>Status:</label>
    <select name="status">
      <% ['UNSTARTED', 'STARTED', 'COMPLETE'].forEach(status => { %>
        <option value="<%= status %>" <% if (status === task.status) { %>selected<% } %>><%= status %></option>
      <% }) %>
    </select>
    <br>
    <label>Class:</label>
    <select name="class">
      <% ['DEFAULT', 'CUSTOM', 'ARCHIVED'].forEach(taskClass => { %>
        <option value="<%= taskClass %>" <% if (taskClass === task.class) { %>selected<% } %>><%= taskClass %></option>
      <% }) %>
    </select>
    <br>
    <button type="submit" onclick="return confirm('Are you sure you want to save changes?');">SAVE</button>
  </form>
</body>
</html>

